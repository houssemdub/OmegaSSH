<UserControl x:Class="OmegaSSH.Views.KeyManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Background="{StaticResource BgBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SSH KEY MANAGER" FontSize="20" FontWeight="Bold" Foreground="{StaticResource CyberCyanBrush}" Margin="0,0,0,20"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Keys}" Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource SidebarBrush}" Margin="0,0,0,10" Padding="15" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Icon="Key" Width="24" Foreground="{StaticResource CyberYellow}" Margin="0,0,20,0" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Type}" Foreground="{StaticResource TextDimBrush}" FontSize="12"/>
                                <TextBlock Text="{Binding PublicKey}" Foreground="{StaticResource TextDimBrush}" FontSize="10" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Button Grid.Column="2" Style="{StaticResource ModernButton}" Content="Copy Public Key" Padding="10,5"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="2" Background="{StaticResource SidebarBrush}" Padding="20" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Key Name" Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewKeyName}" Width="150" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Comment" Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewKeyComment}" Width="200" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <Button Style="{StaticResource ModernButton}" Command="{Binding GenerateKeyCommand}" Content="Generate New RSA Key" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
