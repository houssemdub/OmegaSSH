<Window x:Class="OmegaSSH.Views.MultiCommanderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Multi-Commander Orchestrator" Height="600" Width="900"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True">
    
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource SidebarBrush}">
                <TextBlock Text="MULTI-COMMANDER ORCHESTRATOR" VerticalAlignment="Center" Margin="15,0" FontWeight="Bold" FontSize="12"/>
                <Button Style="{StaticResource IconButton}" Content="" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right" Click="Close_Click"/>
            </Grid>

            <!-- Command Input -->
            <Border Grid.Row="1" Background="{StaticResource SidebarBrush}" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding Command}" Style="{StaticResource ModernTextBox}" Tag="Enter command to run on all selected sessions..." Height="40" FontSize="14"/>
                    <Button Grid.Column="1" Content="EXECUTE PARALLEL" Style="{StaticResource ModernButton}" Command="{Binding ExecuteParallelCommand}" Margin="10,0,0,0" Width="200"
                            IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Session Selection -->
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="SELECT TARGETS" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextDimBrush}" Margin="15,10"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding Sessions}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Session.Name}" Foreground="{StaticResource TextBrush}" Margin="5"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Results -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="EXECUTION LOG" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextDimBrush}" Margin="15,10"/>
                    <ScrollViewer Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding Results}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SidebarBrush}" Margin="10,5" Padding="10" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                        <StackPanel>
                                            <Grid>
                                                <TextBlock Text="{Binding Session.Name}" FontWeight="Bold" Foreground="{StaticResource PrimaryAccentBrush}"/>
                                                <fa:ImageAwesome Icon="Check" Width="12" Foreground="#00FF00" HorizontalAlignment="Right" Visibility="{Binding Success, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <fa:ImageAwesome Icon="Warning" Width="12" Foreground="#FF0000" HorizontalAlignment="Right" Visibility="{Binding Success, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding Output}" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="#AAA" Visibility="{Binding Success, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding Error}" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="#FF4444" Visibility="{Binding Success, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <!-- Status Footer -->
            <Border Grid.Row="3" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <Grid Margin="15,0">
                    <ProgressBar Value="{Binding Progress}" Height="4" VerticalAlignment="Bottom" Background="Transparent" BorderThickness="0" Foreground="{StaticResource HighlightAccentBrush}"/>
                    <TextBlock Text="Ready" VerticalAlignment="Center" FontSize="10" Foreground="{StaticResource TextDimBrush}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
