<Window x:Class="OmegaSSH.Views.SnippetEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Snippet Editor" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        x:Name="ThisWindow">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" CornerRadius="0" ResizeBorderThickness="8"/>
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <fa:ImageAwesome Icon="Code" Width="20" Foreground="{DynamicResource SecondaryAccentBrush}" Margin="0,0,12,0"/>
                <TextBlock Text="SNIPPET EDITOR" FontSize="18" FontWeight="Bold"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="Snippet Name" Style="{StaticResource ModernLabel}"/>
                <TextBox Text="{Binding Name}" Style="{StaticResource ModernTextBox}" Tag="e.g. Update System"/>

                <TextBlock Text="Command / Script" Style="{StaticResource ModernLabel}" Margin="0,15,0,0"/>
                <TextBox Text="{Binding Command}" Style="{StaticResource ModernTextBox}" Height="100" 
                         AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10"
                         Tag="Enter SSH commands..."/>

                <TextBlock Text="Description" Style="{StaticResource ModernLabel}" Margin="0,15,0,0"/>
                <TextBox Text="{Binding Description}" Style="{StaticResource ModernTextBox}" Tag="Optional description"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="CANCEL" Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=ThisWindow}" 
                        Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="15,8" Margin="0,0,10,0"/>
                <Button Content="SAVE SNIPPET" Command="{Binding SaveCommand}" CommandParameter="{Binding ElementName=ThisWindow}" 
                        Style="{StaticResource ModernButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
