<UserControl x:Class="OmegaSSH.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OmegaSSH.ViewModels"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:OmegaSSH.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TerminalRichTextBox" TargetType="RichTextBox">
            <Setter Property="Background" Value="#050505"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" x:Name="SftpColumn"/>
        </Grid.ColumnDefinitions>

        <!-- Terminal Sidebar Toggle (Hover area or button) -->
        <Button Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Top" Panel.ZIndex="10"
                Style="{StaticResource IconButton}" Click="ToggleSftp_Click" Margin="0,15,20,0">
            <fa:ImageAwesome Icon="FolderOpen" Width="14" Foreground="{StaticResource TextDimBrush}"/>
        </Button>

        <Border Grid.Column="0" Background="#050505" Margin="5" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <RichTextBox x:Name="OutputBox" Style="{StaticResource TerminalRichTextBox}">
                    <FlowDocument PagePadding="0"/>
                </RichTextBox>

                <Border Grid.Row="1" Background="{StaticResource SidebarBrush}" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}" Padding="5">
                    <Grid>
                        <TextBox x:Name="InputBox" 
                                 Style="{StaticResource ModernTextBox}" 
                                 KeyDown="InputBox_KeyDown"
                                 Tag="Type command and press Enter..."/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- SFTP Pane -->
        <Border Grid.Column="1" Width="300" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0" x:Name="SftpPane" Visibility="Collapsed">
            <local:SftpView DataContext="{Binding Sftp}"/>
        </Border>
    </Grid>
</UserControl>
